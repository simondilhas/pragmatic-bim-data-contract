# Submodule Placeholder

Initialize the private classifications submodule:

```bash
git submodule update --init --recursive
```

Expected remote:

- `git@github.com:simondilhas/pragmatic-bim-classifications.git`
